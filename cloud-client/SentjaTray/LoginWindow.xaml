<Window x:Class="SentjaTray.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sentja Cloud — Login"
        Width="380" Height="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">

  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Logo & Title -->
    <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,28">
      <Border Width="56" Height="56" CornerRadius="14" Margin="0,0,0,12"
              HorizontalAlignment="Center">
        <Border.Background>
          <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#667eea" Offset="0"/>
            <GradientStop Color="#764ba2" Offset="1"/>
          </LinearGradientBrush>
        </Border.Background>
        <TextBlock Text="S" FontSize="28" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Border>
      <TextBlock Text="Sentja Cloud" FontSize="22" FontWeight="Bold"
                 Foreground="White" HorizontalAlignment="Center"/>
      <TextBlock Text="Sign in to your account" FontSize="13"
                 Foreground="#aaaacc" HorizontalAlignment="Center" Margin="0,4,0,0"/>
    </StackPanel>

    <!-- Error message -->
    <Border Grid.Row="1" x:Name="ErrorBorder" Background="#3d1a1a" CornerRadius="8"
            Padding="12,10" Margin="0,0,0,12" Visibility="Collapsed">
      <TextBlock x:Name="ErrorText" Foreground="#f56565" FontSize="13" TextWrapping="Wrap"/>
    </Border>

    <!-- Email -->
    <StackPanel Grid.Row="2" Margin="0,0,0,14">
      <TextBlock Text="Email" Foreground="#aaaacc" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
      <TextBox x:Name="EmailBox" FontSize="14" Padding="12,10"
               Background="#2d2d4e" Foreground="White" BorderBrush="#3d3d5e"
               BorderThickness="1" CaretBrush="White">
        <TextBox.Resources>
          <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
        </TextBox.Resources>
      </TextBox>
    </StackPanel>

    <!-- Password -->
    <StackPanel Grid.Row="3" Margin="0,0,0,20">
      <TextBlock Text="Password" Foreground="#aaaacc" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
      <PasswordBox x:Name="PasswordBox" FontSize="14" Padding="12,10"
                   Background="#2d2d4e" Foreground="White" BorderBrush="#3d3d5e"
                   BorderThickness="1" CaretBrush="White">
        <PasswordBox.Resources>
          <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
        </PasswordBox.Resources>
      </PasswordBox>
    </StackPanel>

    <!-- Login Button -->
    <Button Grid.Row="4" x:Name="LoginButton" Content="Sign In"
            Height="44" FontSize="15" FontWeight="SemiBold"
            Foreground="White" BorderThickness="0" Cursor="Hand"
            Click="OnLoginClick">
      <Button.Template>
        <ControlTemplate TargetType="Button">
          <Border CornerRadius="10" Background="{TemplateBinding Background}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
        </ControlTemplate>
      </Button.Template>
      <Button.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
          <GradientStop Color="#667eea" Offset="0"/>
          <GradientStop Color="#764ba2" Offset="1"/>
        </LinearGradientBrush>
      </Button.Background>
    </Button>

    <!-- Status -->
    <TextBlock Grid.Row="5" x:Name="StatusText" Text=""
               Foreground="#aaaacc" FontSize="12" HorizontalAlignment="Center"
               Margin="0,12,0,0"/>
  </Grid>
</Window>
