<Window x:Class="SentjaTray.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings — Sentja Cloud"
        Width="460" Height="380"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Settings"
               FontSize="20" FontWeight="Bold" Foreground="#1a1a2e" Margin="0,0,0,16"/>

    <StackPanel Grid.Row="1">
      <!-- API URL -->
      <StackPanel Margin="0,0,0,14">
        <TextBlock Text="API Server URL" FontSize="12" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,6"/>
        <TextBox x:Name="ApiUrlBox" FontSize="13" Padding="10,8" BorderBrush="#ddd"/>
      </StackPanel>

      <!-- Sync Root -->
      <StackPanel Margin="0,0,0,14">
        <TextBlock Text="Sync Root Path" FontSize="12" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,6"/>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBox x:Name="SyncRootBox" FontSize="13" Padding="10,8" BorderBrush="#ddd" Grid.Column="0"/>
          <Button Content="Browse" Grid.Column="1" Margin="6,0,0,0" Padding="10,8"
                  Click="OnBrowseSyncRoot" Cursor="Hand"/>
        </Grid>
      </StackPanel>

      <!-- Cache Size -->
      <StackPanel Margin="0,0,0,14">
        <TextBlock Text="Max Cache Size (GB)" FontSize="12" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,6"/>
        <TextBox x:Name="CacheSizeBox" FontSize="13" Padding="10,8" BorderBrush="#ddd"/>
      </StackPanel>

      <!-- Heartbeat Interval -->
      <StackPanel>
        <TextBlock Text="Heartbeat Interval (seconds)" FontSize="12" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,6"/>
        <TextBox x:Name="HeartbeatBox" FontSize="13" Padding="10,8" BorderBrush="#ddd"/>
      </StackPanel>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancel" Padding="16,8" Margin="0,0,8,0" Click="OnCancel" Cursor="Hand"/>
      <Button Content="Save" Padding="16,8"
              Background="#1a1a2e" Foreground="White" BorderThickness="0"
              Click="OnSave" Cursor="Hand"/>
    </StackPanel>
  </Grid>
</Window>
